// const db = require('./database/');

module.exports = {
  getReviewsList: (productID, page, count, sort) => {},
  getMeta: productID => {},
  addReview: body => {},
  setHelpful: reviewID => {},
  reportReview: reviewID => {}
};
